#include <stdio.h>
#include <stdlib.h>
#include "elec.h"

int main()
{
    uint32_t A = (3 << 30) + (3 << 23) + (1<20);
    uint32_t B = (1<<30) + (1<<24) + (3<<19);
    uint32_t C = ;
    if (IEEE_mult(A, B) ==)
    printf("%d", C);
    return 0;
}
